import { FilmsItemResolver } from './films-item.resolver';

describe('FilmsItemResolver', () => {
  it('should create an instance', () => {
    expect(new FilmsItemResolver()).toBeTruthy();
  });
});
