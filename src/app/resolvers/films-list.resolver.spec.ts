import { FilmsListResolver } from './films-list.resolver';

describe('FilmsListResolver', () => {
  it('should create an instance', () => {
    expect(new FilmsListResolver()).toBeTruthy();
  });
});
