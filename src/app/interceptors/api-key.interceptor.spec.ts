import { ApiKeyInterceptor } from './api-key.interceptor';

describe('ApiKeyInterceptor', () => {
  it('should create an instance', () => {
    expect(new ApiKeyInterceptor()).toBeTruthy();
  });
});
